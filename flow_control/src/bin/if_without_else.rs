fn main() {
    compute(10);
}

fn compute(value: i32) -> bool {
    // if value >= 0 {
    //     true
    // };
    // Error! if without else was evaluate as `()` TODO ^ Try uncomment this line
    false
}